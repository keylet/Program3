
### 3. README for the JavaScript Program (Even or Odd Number)

```markdown
# Even or Odd Number - JavaScript

This JavaScript program determines whether an input number is even or odd.

## Requirements

- Docker

## Instructions

1. Save the `program3.js` file and the `Dockerfile` in the same directory.
2. Build the Docker image:

   ```bash
   docker build -t js-program3 .
